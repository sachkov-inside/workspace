# Sachkov Inside

- Private Membership product/operations control plane; canonical repository instructions: `AGENTS.md`.
- Tracker authority: private GitHub Issues in `KirillSachkov/sachkov-inside`; exact contract: `docs/agents/issue-tracker.md`.
- Product glossary: `CONTEXT.md`.
- Module authorities: `product/`, `operations/`, `portfolio/`, `research/`.
- `sachkov-content` owns scripts, videos, lessons, posts and publication workflow; this repo stores portfolio decisions/status/links only.
- Future Inside application is a separate owner-created repository; do not specify or implement it here before build-series decisions.
- External actions (payment activation, publishing, invitations/removals, messages, production mutation) always require explicit owner GO.
- Tooling and runtime facts: `mem:tech_stack`; common commands: `mem:suggested_commands`; writing and authority rules: `mem:conventions`; completion gates: `mem:task_completion`.