# Task completion

- Active GitHub Issue acceptance is met without scope expansion.
- Run `bash scripts/verify-workspace.sh` and `git diff --check`.
- Read affected Issue and both ends of every relation back after writes.
- Verify linked `sachkov-content` authority before claiming content production state.
- No secrets, payment data, participant PII or private invites in repo/Issues.
- External actions have explicit owner GO and safe read-back evidence.
- Deliver exactly one complete project-task report in the canonical task surface; owner controls merge to `main`.