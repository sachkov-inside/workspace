Generate two or three short initial questions from the supplied CaseSpec, exact cumulative diff,
source facts, technical report/telemetry and Decision Record. Participant answers are deliberately
absent from this call. Do not assess mastery yet.

Prefer the smallest set that probes the highest-risk mismatch, uncertainty or trade-off. Every
question must be specific to this attempt; generic theory questions are invalid. Cite exact input
fact IDs through `groundedIn` and return JSON matching the question schema.
